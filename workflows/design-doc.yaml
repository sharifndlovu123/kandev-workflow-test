version: 1
type: kandev_workflow
workflows:
  - name: Design Doc
    description: >-
      Turn a rough feature/product idea into an approved design document
      committed to the repo's design-doc directory. Draft -> independent design
      review(s) -> human approval -> PR (the human merges it). Same control
      shape as "Feature Delivery" minus the implementation half. The output doc
      becomes immutable input to a later Feature Delivery task.
    prompt: |
      ## Kandev navigation protocol (applies to every step)

      You are one step of a Kandev workflow task. Your task_id and workflow_id are
      in the session context.

      The persistent task PLAN (create_task_plan_kandev / get_task_plan_kandev /
      update_task_plan_kandev) is the single source of truth for this task. The
      DESIGN DOC being produced lives IN the plan until the final Commit step
      writes it to a file. Verdicts and the round counter live there too. Never
      rely on chat history - it is reset between steps.

      Each step's prompt starts with either a MOVEMENT DISCIPLINE block (you choose
      the next step) or a STEP COMPLETION block (the workflow advances you). Follow
      whichever THIS step has.

      MOVEMENT DISCIPLINE steps — to move the task on:
        1. list_workflow_steps_kandev(workflow_id)  -> step names to ids
        2. move_task_kandev(task_id, workflow_id, workflow_step_id=<target id>,
           prompt="<short factual handoff, <= 2 sentences>")
        3. STOP. That call is the LAST action of your turn. Call it exactly ONCE,
           then end your turn. Do NOT keep working, do NOT advance the task past the
           step you moved it to - a step that runs no agent (e.g. "Human Approval")
           is a DELIBERATE HUMAN GATE; leave the task there.

      STEP COMPLETION steps — do the step's work, then call step_complete_kandev and
      end your turn. Do NOT call move_task_kandev on these steps. If you are blocked,
      do not signal — just end your turn and the task waits for a human.

      Never argue your case in a move `prompt`. A fresh-context reviewer forms its
      own judgment from the plan and the repo. All substance goes in the plan.

    steps:
      - name: Backlog
        position: 0
        color: bg-neutral-400
        is_start_step: false
        show_in_command_panel: false
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        events: {}

      - name: Draft Doc
        position: 1
        color: bg-purple-500
        is_start_step: true
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: true
        cancel_triggers_turn_complete: false
        agent_profile:
          agent_name: Claude
          model: sonnet
          mode: acceptEdits
        events:
          on_enter:
            - type: enable_plan_mode
            - type: auto_start_agent
          on_turn_complete:
            - type: move_to_next
          on_exit:
            - type: disable_plan_mode
        prompt: |
          [STEP COMPLETION - this step advances on its own]
          - You do NOT move the task and you do NOT call move_task_kandev. When your
            work for THIS step is finished, call step_complete_kandev and end your
            turn - the workflow then advances the task to the next step ("Review -
            Design") automatically.
          - Calling move_task_kandev here is an error; it can skip a gate.
          - If the ask is ambiguous on something that changes the design, or a human
            must decide: do NOT call step_complete_kandev - just end your turn (after
            calling ask_user_question_kandev if it is a question). The task stays on
            this step.
          - PLAN MODE IS ON here: you cannot create or edit any file. Write only into
            the task plan; a later step writes files. Ignore any note telling you to
            write or commit a file here.

          [DRAFT DOC - produce a design document]
          Plan mode is ON: you cannot change files. This phase is research + writing
          the doc into the task plan only.

          {{task_prompt}}

          ROUND CHECK: the plan may already contain "Revision round: N" and one or
          more "## Design review round K - REJECTED" sections. If so this is a
          revision: read every objection, address each, increment the counter.
          Otherwise this is round 1.

          1. UNDERSTAND THE ASK. What problem, who benefits, what constraints,
             what does success look like. If the ask is ambiguous on something
             that changes the design, call ask_user_question_kandev and stop.
          2. SKILLS. Invoke `architecture-review` and `clean-code`.
          3. EXPLORE. If a codebase exists, map the relevant architecture,
             patterns, integration points, and any existing docs/decisions. A
             design doc must fit the system that exists.
          4. DERIVE A SLUG. A short kebab-case name for this feature (e.g.
             "user-invites", "rate-limiting"). The doc will be committed into the
             repo's design-doc directory (detect it: an existing `docs/**/specs/`
             such as `docs/superpowers/specs/`, else `docs/specs/`), typically as
             `<YYYY-MM-DD>-<slug>-design.md` if that repo's existing docs use a
             date prefix, else `<slug>.md` - match the repo's own convention.
             Record the slug AND the chosen target path near the top of the plan.
          5. WRITE THE DESIGN DOC into the plan (create_task_plan_kandev, or
             update_task_plan_kandev on a revision). Structure:
             - # Feature: <name>
             - ## Why  (problem, who benefits, motivating scenarios)
             - ## Scope  (what is in; a bulleted "Explicitly out of scope")
             - ## Design  (2-3 approaches with trade-offs - name what each gives
               up - and a recommendation; the data/state model; key components and
               their responsibilities; mermaid diagrams for architecture/flow)
             - ## Behaviour  (each behaviour as a concrete, testable statement)
             - ## Risks & second-order effects
             - ## Success criteria
             Put "Revision round: N" and "slug: <slug>" near the top.
             Do NOT create any file - the doc lives in the plan for now.
          6. SIGNAL DONE: call step_complete_kandev, then end your turn. The workflow
             advances the task to "Review - Design" (first of two independent reviews:
             Claude on the doc's soundness, then Codex on the approach).

      # First of two independent design reviews. Fresh Claude session; soundness lens.
      - name: Review - Design
        position: 2
        color: bg-cyan-500
        is_start_step: false
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        agent_profile:
          agent_name: Claude
          model: sonnet
          mode: acceptEdits
        events:
          on_enter:
            - type: reset_agent_context
            - type: auto_start_agent
        prompt: |
          [MOVEMENT DISCIPLINE - read before acting]
          - Move the task to EXACTLY ONE step: the target named in THIS step's own
            instructions below. Never a step further ahead. Never "Done", "Commit Doc",
            "Commit", "Integrate", or "Needs Human" unless THIS prompt names it as your
            target.
          - The move is ONE tool call, made once, as the final action of your turn:
              move_task_kandev(task_id, workflow_id, workflow_step_id=<id>, prompt="<handoff>")
            Get <id> from list_workflow_steps_kandev(workflow_id) - it is the target
            step's id, NOT its name. The handoff argument is named `prompt`; there is
            no `message` and no `step` argument. After the call, end the turn - do not
            call move_task_kandev again and do not keep working.
          - If you are unsure where to go, STOP and end the turn WITHOUT moving - a
            human will unstick you. Do not guess forward.
          - Steps that run no agent (Human Approval / Human Review / Done) are hard
            gates. Moving a task into or past one is a serious error. Nothing
            structural catches it - this block is the only guard.

          [DESIGN REVIEW - fresh session, first of two independent review gates]
          Fresh session. No memory of prior rounds. You are the FIRST of two
          independent reviewers (you, then Codex). Review as an independent critic;
          do not assume the drafter got it right.

          1. READ: get_task_plan_kandev (doc + all prior review rounds); explore
             the codebase if one exists. Note "Revision round: N".
          2. REVIEW: invoke `architecture-review`. Your lens is SOUNDNESS AND
             COMPLETENESS (Codex takes the approach/risk lens next):
             - internal coherence; the design actually solves the stated problem
             - architecture fit against the system that exists
             - completeness: every behaviour a concrete testable statement; edge
               cases and failure paths named
             - trade-off honesty: each approach's cost is stated, the pick is
               justified
             - every objection in earlier "## Design review round" sections is
               actually resolved
          3. VERDICT: update_task_plan_kandev, append:
               ## Design review round N - APPROVED|REJECTED by Claude Design Review
               (REJECTED: numbered objections with why + fix. APPROVED: one line.)
          4. ROUTE (one move, then end your turn):
             - APPROVED -> move to "Review - Codex". Handoff: "Claude design review
               passed, round N."
             - REJECTED and N < 3 -> move to "Draft Doc". Handoff: "Rejected round N."
             - REJECTED and N >= 3 -> move to "Needs Human".
               Handoff: "Stalled round N: <one-line core disagreement>."

      # Second, independent design review by a different vendor (Codex). Approach/risk
      # lens; last gate before Human Approval. Bind the `codex` profile in the UI.
      - name: Review - Codex
        position: 3
        color: bg-sky-500
        is_start_step: false
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        agent_profile:
          agent_name: Codex
          model: gpt-5.6-sol
          mode: agent
        events:
          on_enter:
            - type: reset_agent_context
            - type: auto_start_agent
        prompt: |
          [MOVEMENT DISCIPLINE - read before acting]
          - Move the task to EXACTLY ONE step: the target named in THIS step's own
            instructions below. Never a step further ahead. Never "Done", "Commit Doc",
            "Commit", "Integrate", or "Needs Human" unless THIS prompt names it as your
            target.
          - The move is ONE tool call, made once, as the final action of your turn:
              move_task_kandev(task_id, workflow_id, workflow_step_id=<id>, prompt="<handoff>")
            Get <id> from list_workflow_steps_kandev(workflow_id) - it is the target
            step's id, NOT its name. The handoff argument is named `prompt`; there is
            no `message` and no `step` argument. After the call, end the turn - do not
            call move_task_kandev again and do not keep working.
          - If you are unsure where to go, STOP and end the turn WITHOUT moving - a
            human will unstick you. Do not guess forward.
          - Steps that run no agent (Human Approval / Human Review / Done) are hard
            gates. Moving a task into or past one is a serious error. Nothing
            structural catches it - this block is the only guard.

          [CODEX DESIGN REVIEW - fresh session, second and final review gate]
          Fresh session, different model vendor. Claude has already reviewed this
          design doc and approved it. You are the SECOND, independent review and
          the last gate before the human - be exacting. Do not assume Claude or the
          drafter got it right.

          Your lens is APPROACH AND RISK, not style:
            - Is this the right approach at all? Name a materially simpler design
              if one exists.
            - Hidden or unstated assumptions the design depends on.
            - Second-order effects: what else in the system this changes; failure
              modes; rollout, migration, backward compatibility; scale.
            - Scope creep vs the stated problem; speculative generality.
            - Risks the drafter and the first reviewer both missed.

          1. READ: get_task_plan_kandev (doc + all prior review rounds, including
             "## Design review round N ... by Claude Design Review"); explore the
             codebase if one exists. Note "Revision round: N".
          2. REVIEW along the lens above.
          3. VERDICT: update_task_plan_kandev, append:
               ## Design review round N - APPROVED|REJECTED by Codex Design Review
               (REJECTED: numbered objections with why + fix. APPROVED: one line.)
          4. ROUTE (one move, then end your turn):
             - APPROVED -> move to "Human Approval". Handoff: "Both design reviews
               passed, round N."
             - REJECTED and N < 3 -> move to "Draft Doc". Handoff: "Codex rejected
               round N."
             - REJECTED and N >= 3 -> move to "Needs Human".
               Handoff: "Stalled round N: <one-line core disagreement>."

      - name: Human Approval
        position: 4
        color: bg-yellow-500
        is_start_step: false
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        events: {}
        prompt: |
          [HUMAN APPROVAL GATE - no agent runs here]
          Sharif reads the design doc in the task plan.
          - Approve -> drag the task to "Commit Doc" (opens a PR; Sharif merges it).
          - Changes -> add a note to the plan and drag it back to "Draft Doc".

      # Doc commit + PR runs on Codex.
      - name: Commit Doc
        position: 5
        color: bg-indigo-500
        is_start_step: false
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: true
        cancel_triggers_turn_complete: false
        agent_profile:
          agent_name: Codex
          model: gpt-5.6-sol
          mode: agent-full-access
        events:
          on_enter:
            - type: auto_start_agent
          on_turn_complete:
            - type: move_to_next
        prompt: |
          [STEP COMPLETION - this step advances on its own]
          - You do NOT move the task and you do NOT call move_task_kandev. When the
            doc PR is open, call step_complete_kandev and end your turn - the workflow
            then advances the task to "Done" automatically. "Done" is terminal.
          - Calling move_task_kandev here is an error; it can skip a gate.
          - If you cannot open the PR (push fails, base branch unclear): record why
            in the plan, end your turn, do NOT call step_complete_kandev. The task
            stays here for a human.

          [COMMIT DOC]
          The human approved the design. Package it as a PR - you do NOT merge it.

          1. get_task_plan_kandev. Read "slug: <slug>", the chosen target path, and
             the doc body.
          2. TARGET PATH: use the path recorded in the plan. If none, detect the
             repo's design-doc directory (an existing `docs/**/specs/` such as
             `docs/superpowers/specs/`, else `docs/specs/`) and match the repo's
             existing filename convention (date-prefixed `<YYYY-MM-DD>-<slug>-design.md`
             if its neighbours use that, else `<slug>.md`). Create the directory if
             needed.
          3. Write the doc body (the "# Feature ..." sections, NOT the round-counter
             or review-round sections) to that path.
          4. BASE BRANCH: the PR target is the branch this task was cut from - take
             it from the task's configuration / the branch this worktree tracks.
             Common here: `dev` (not `main`, never `cloud-env` / a prod branch).
          5. Commit on a new branch with an explicit path:
             git checkout -b docs/<slug>
             git add <target path>
             git commit -m "docs: <slug> design doc"
          6. Push and open a PR against the base branch (NOT --draft):
             gh pr create --base <base branch> --title "docs: <slug> design doc"
               --body "<one-paragraph summary of the design>"
             DO NOT run `gh pr merge`. DO NOT merge, approve, or close the PR. The
             doc is the source of truth for all future work on this feature -
             Sharif merges it deliberately after a final read. Merging it yourself
             is a serious error.
          7. Record the PR URL in the plan. Then call step_complete_kandev and END
             YOUR TURN immediately. Do not do any cleanup. The workflow moves the
             task to "Done" - terminal, you are finished.

      - name: Done
        position: 6
        color: bg-green-500
        is_start_step: false
        show_in_command_panel: false
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        auto_archive_after_hours: 168
        events: {}

      - name: Needs Human
        position: 7
        color: bg-red-500
        is_start_step: false
        show_in_command_panel: true
        allow_manual_move: true
        auto_advance_requires_signal: false
        cancel_triggers_turn_complete: false
        events: {}
        prompt: |
          [NEEDS HUMAN - the design review loop hit its 3-round cap]
          Read the plan's "## Design review round" sections for the disagreement.
          Then edit the plan and drag to the right step, or archive the task.
